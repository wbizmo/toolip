# Static Report Publishing

Generate a report:

```bash
toolip publish
```

Toolip writes a static HTML report to `.toolip-report/index.html`. Review the report before deploying it to GitHub Pages or any public host.
