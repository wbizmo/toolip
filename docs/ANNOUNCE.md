# Announcement Generation

```bash
toolip announce --version 2.0.0 --fixed 4 --added 1 --removed 2
```

The output is deterministic and local. Toolip does not call an AI service or invent release claims.
