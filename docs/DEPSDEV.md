# deps.dev Package Intelligence

Toolip uses the deps.dev v3 API to inspect exact npm package versions.

## Command

```bash
toolip package-health express 5.0.0
```

The command reports published date, deprecation, SPDX license identifiers, advisory identifiers, dependency graph size, related projects, verified provenance, and verified attestations when available.
