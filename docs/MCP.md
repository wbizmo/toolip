# Toolip MCP Server

Start the server:

```bash
toolip mcp
```

The MCP server uses stdio and exposes read-only tools for the security doctor, SBOM generation, and Git security diffs. It does not expose arbitrary shell execution or automatic write operations.
