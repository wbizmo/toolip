# Toolip Configuration

Toolip uses `toolip.config.json`.

## Initialize

```bash
toolip config init
toolip config validate
toolip config show
```

Configuration supports include/exclude paths, rule enablement, severity overrides, path-specific policy, expiring suppressions with reasons, history retention, and provider settings.
